{
  "name": "smart-contracts",
  "version": "1.0.0",
  "scripts": {
    "start": "truffle compile && truffle migrate --network development",
    "prestart": "ganache-cli -p 8545 &",
    "install": "npm truffle ganache-cli",
    "test": "concurrently \"ganache-cli -p 8545 \" \"truffle compile && truffle test\""
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "concurrently": "^5.3.0"
  }
}
