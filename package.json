{
  "name": "smart-contracts",
  "version": "1.0.0",
  "scripts": {
    "start": "truffle compile && truffle migrate --network development",
    "prestart": "ganache-cli -p 8545 &",
    "ganache": "ganache-cli -p 8545",
    "test": "truffle compile && truffle test --show-events"
  },
  "dependencies": {
    "truffle-assertions": "^0.9.2",
    "web3": "^1.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "concurrently": "^5.3.0"
  }
}
